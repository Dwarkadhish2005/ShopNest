# ─── LLM Provider ────────────────────────────────────
# 'groq'   = FREE ✅  (recommended)
# 'openai' = paid
LLM_PROVIDER=groq

# ─── Groq (FREE — https://console.groq.com) ──────────
# Sign up free, create an API key, paste it below:
GROQ_API_KEY=your_groq_api_key_here

# Optional: change Groq model (default below is recommended)
GROQ_MODEL=llama-3.3-70b-versatile

# ─── OpenAI (paid fallback — set LLM_PROVIDER=openai) ─
OPENAI_API_KEY=your_openai_key_here
LLM_MODEL=gpt-3.5-turbo

# ─── API Runtime ───────────────────────────────────────
API_HOST=127.0.0.1
API_PORT=8000

# ─── Observability ─────────────────────────────────────
LOG_LEVEL=INFO
ENABLE_PHOENIX=false

# Phoenix Cloud API key (from app.phoenix.arize.com)
PHOENIX_API_KEY=

# Optional: set if using remote/local Phoenix collector
PHOENIX_COLLECTOR_ENDPOINT=
